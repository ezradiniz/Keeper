import React from 'react';
import { Tooltip } from 'react-bootstrap';

export const removeTip = (
  <Tooltip id='removeTip'>Remove Note</Tooltip>
);

export const archiveTip = (
  <Tooltip id='archiveTip'>Archive Note</Tooltip>
);

export const restoreTip = (
  <Tooltip id='restoreTip'>Restore Note</Tooltip>
);

export const shareTip = (
  <Tooltip id='shareTip'>Share Note</Tooltip>
);

export const updateTip = (
  <Tooltip id='updateTip'>Edit Note</Tooltip>
);
